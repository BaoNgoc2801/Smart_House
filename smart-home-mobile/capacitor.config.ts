import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.smarthome',
  appName: 'smart-home-mobile',
  webDir: 'dist'
};

export default config;
